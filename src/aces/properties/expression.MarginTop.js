export const config = {
  highlight: false,
  returnType: "number",
  description: "Margin top",
  params: [],
};

export const expose = false;

export default function () {
  return this.marginTop;
}
