export const config = {
  highlight: false,
  returnType: "number",
  description: "Margin right",
  params: [],
};

export const expose = false;

export default function () {
  return this.marginRight;
}
