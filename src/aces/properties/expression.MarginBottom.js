export const config = {
  highlight: false,
  returnType: "number",
  description: "Margin bottom",
  params: [],
};

export const expose = false;

export default function () {
  return this.marginBottom;
}
