export const config = {
  highlight: false,
  returnType: "number",
  description: "Max overscroll",
  params: [],
};

export const expose = false;

export default function () {
  return this.maxOverScroll;
}
