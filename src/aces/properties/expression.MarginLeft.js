export const config = {
  highlight: false,
  returnType: "number",
  description: "Margin left",
  params: [],
};

export const expose = false;

export default function () {
  return this.marginLeft;
}
